'use strict';

//TODO export this and import it into ../create-db.js

/**
 -	FUNCTION Name: createBillTransaction()
 -	FUNCTION Description: Given a BillID, create a transaction. The timestamp for this transaction will be generated when it is created.
 -	Input Parameter: BillID (integer), transactionDescription (string), transactionAmount,
 -	Optional Parameter: BillDescription (string)
 -	Returns: Adds a new transaction to the specified bill
 **/
const createBillTransaction = function(customer) {

};